const initialState = {
  list: []
}

const productTop = (state = initialState, action) => {
  switch(action.type) {
    case "ADD_PRODUCT_TOPS" : { 
      return state
    }
    default: {
      return state
    }
  }
}

export default productTop